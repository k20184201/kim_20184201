import React from "react";
import TodoListItem from "./11.4-1(TodoListItem)";
import "./10.2.3-4(TodoList).scss";

const TodoList = ({ todos, onRemove, onToggle }) => {
  return;
};

export default React.memo(TodoList);
